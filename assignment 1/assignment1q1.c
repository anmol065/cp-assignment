#include<stdio.h>
int main()
{
	int length,breadth,area;
	printf("enter length and breadth of rectangle");
        scanf("%d\n%d",&length,&breadth);
	area=length*breadth;
        	printf("area=%d",area);
}
